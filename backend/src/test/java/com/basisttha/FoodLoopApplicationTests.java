package com.basisttha;

/**
 * Placeholder — this file was retained because renaming it would require a
 * Maven source-set change.  Actual tests live in {@code CrdtTest.java}.
 *
 * The original class was named after a different project ("FoodLoop") and
 * carried a {@code @SpringBootTest} that required a running database.  Both
 * issues are fixed: the class is now a no-op, and the context/integration tests
 * are placed in a dedicated test class.
 */
class FoodLoopApplicationTests {
    // Intentionally empty — see CrdtTest for unit tests.
}
